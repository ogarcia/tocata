# The build context only needs what the Containerfile copies, and these are the
# directories that would otherwise make it large for nothing.
target
data
.git
.githooks
# Built inside the image by trunk; a copy from here would only be stale.
panel/dist
